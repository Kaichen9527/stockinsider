# Follow-through: source discovery, parser and disk

## Production changes and observations

- Deleted exactly five unused release directories after checking current/previous,
  `/proc` descriptors/maps/command lines, Docker mounts, mountinfo and service/cron
  configuration references. Deleted hashes: 01eeff93e68132c692925e7876eb23a0cd56c619,
  0c0236c7ba374c8a91ecde92b2c115e1b4c0f923, 3c3ae4716f5ecab096c25340f72876f72365b6b5,
  5e5158982befa472de3bbe76bc7dbf3becfb62e8, b865ea3126dfbde2221858169c9fed0daf3ff5f4.
  Current 408c15c and previous 801b20a retained. Free disk grew from about 6G to 10G;
  web service remained active. No containers, volumes, databases or other sites removed.
- Copied the two unchanged current-release parser scripts into the credential-free
  runtime/app directory, verified matching SHA-256s, installed the corrected parser
  service/socket units and validated them with systemd-analyze. Saved prior units
  in `/tmp/stockinsider-parser-repair.VXQMr8`. Web release unchanged.
- Socket now activates but real subprocess tests exposed a second failure: Arelle
  attempts to create `/.config` under the DynamicUser. Reproduced exact traceback
  under that account; root execution returns a hash-bound partial result instead.
- Implemented per-request temporary XDG_CONFIG_HOME in the socket launcher and
  ran the real offline subprocess regression locally with HOME=/ and a sentinel
  credential. Config is deleted and credentials are not inherited. This latest
  socket code has not yet been installed on VPS at the time of this report.
- Real Threads TOP searches for 股票/台股/ThreadsAPI on graph.threads.net and
  graph.threads.com each returned HTTP 200 with zero rows. No connector enabled,
  no invented authors or mentions, no cookies/private endpoints. The permissions
  probe returned HTTP 500/code 100, which does not prove granted or missing scopes.

## Later production repair and exact remaining parser failure

- Installed Arelle's supported package `config/XDG_CONFIG_HOME` pointer to
  `/tmp/stockinsider-arelle-config`, after verifying PrivateTmp=yes and UMask=0077.
  This fixes the existing unchanged production parser without exposing credentials.
  Parser service/socket and web are all active. Real stored TSMC Q2 bytes now return
  hash-bound `partial`, 200 locators and `arelle_validation_errors`, not a subprocess
  crash. No financial facts were accepted by this diagnostic.
- Downloaded the official 2026-03-31 taxonomy from
  https://mopsov.twse.com.tw/nas/taxonomy/tifrs-20260331.zip and inspected the bounded
  archive before local extraction. Offline probe retains the exact original filing
  SHA-256 351b4e59781a71504b30cec9d977e060ab4f978d31695e027252ecfe793d1e55.
- Exact schema mapping alone left relative imports resolving against the upload
  directory (2,677 validation errors). Relocating unchanged bytes beside a private
  taxonomy copy eliminates missing taxonomy IO errors, but leaves 801 validation
  errors: XHTML element structure, missing inline tuple order, element occurrences,
  and an Arelle AttributeError. Thus taxonomy installation alone is NOT a valid
  completion claim. Need a separately validated official alternative document or
  explicitly reviewed Taiwan-inline compatibility adapter; do not suppress errors.
- Product correctness: 150/150 passed. Local candidate runtime: 198/198 and
  migration/contracts: 38/38 previously passed in this follow-through.

## Branch additions

- Public Threads search no longer excludes every non-seeded author. Required
  keywords, unique-post author summaries, monitor-only KOL discovery and preservation
  of existing profile decisions added. Search canary evidence survives run metadata
  updates. This is discovery evidence, never proven trading performance.
- Added guarded document retry API and private append-only retry audit. Only zero-fact
  runtime outages may requeue; integrity errors cannot. Idempotent request IDs and
  three-attempt cap preserve failed evidence. Local PostgreSQL tests passed.
- CI installs pinned parser test dependencies to exercise the real isolated subprocess
  rather than skip it.

## Still not complete

No new web deployment/migration, no completed full financial/history backfill, no
complete issuer-specific valuation dossier rollout, and no successful Threads
public-search/mention canary. PR remains subject to genuine exact review.
