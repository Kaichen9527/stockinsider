# StockInsider provisions its own named PostgreSQL 17 cluster after package
# installation. Prevent the Debian package from creating and starting an
# unreviewed `main` cluster on port 5432 during installation.
create_main_cluster = false
