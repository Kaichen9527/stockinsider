# Independent candidate-code findings repaired

Native review 5325223908 found two P2 issues, no P0/P1. The exact permission
profile in contract section 8 now includes the escaped operation-parent and
transport deny keys and all four validated substitutions. A new regression
renders that exact normative block with quoted paths and compares every byte to
the actual adapter. All 28 noncredential Node tests pass; these are not the
28 registered protected model-gate cases. No live model proof is claimed.

The build log's CR/trailing spaces made the mandated full-range diff check fail.
Its original bytes are now retained losslessly in candidate-web-build-original.json
with base64, original byte length and original SHA256. candidate-web-build.txt
is a separately hashed normalized display, not a replacement claim about raw
stdout. The earlier commit retains the original text as well. The representation
relationship is explicit in candidate-validation.json. The build was not rerun
or its outcome changed for this evidence-only transformation.

Full `git diff --check` against protected main passed after the repair. Runtime
adapter/fixture/canary behavior is unchanged from the nine-check passing receipt.
No assertion, protected worker, host-successor mapping, credential, production
state or release gate has been relaxed. The current candidate still requires
its own final independent source review and genuine base-first activation.
