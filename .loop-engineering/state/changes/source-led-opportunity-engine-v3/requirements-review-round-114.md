# Fresh Requirements Gate Review — Round 114

Subject commit: `d756ff2b9a2866adfc0eccb8d66787350292823b`
Subject tree: `3bfbbbf16d0ab1dc7c5c5a218a987bac43d00ba7`
Baseline commit: `1b714a4303c2a97ebb913dced8d9d607a4ac8951`
Baseline tree: `463cdf53127a38553ecba80f31ad8cddb28ff6f1`
Reviewer: independent read-only `gpt-5.6-sol` / xhigh process
Verdict: `CHANGES_REQUIRED` (`P0=0`, `P1=7`, `P2=0`)

## P1 findings

1. Mixed `unchanged + rejected` source documents fell through to `no_new_items`
   even though successful-empty is reserved for a zero-document result.
2. A valid YouTube HTTP 200 channel lookup with no channel item emitted
   `missing_endpoint`, which violates the connector-attempt storage matrix.
3. Valuation roster and taxonomy membership selected provider business time before
   database `recorded_at`, so a later-recorded inactive correction could be revived.
4. Projection freshness used the same non-authoritative calendar ordering and did not
   fail closed on an equal-latest semantic conflict.
5. Missing taxonomy could be coerced to `unknown` and eight unrelated unknown-sector
   equities could manufacture peer authority.
6. Relative valuation could become available without fresh official
   `shares_outstanding`, using an equal-weighted substitute rather than the required
   official market-cap weighting.
7. The fixed source-plane catalog oracle stored only default presence and partial
   trigger identity, not exact default expressions, enabled state, trigger definition
   and predicate.

## Required closure

- Apply the full database-owned terminal precedence and exercise mixed unchanged plus
  rejected documents through the applied completion RPC.
- Normalize an observed YouTube 2xx zero-item response to `successful_empty` while
  retaining its response evidence.
- Use one latest-recorded, equal-head-conflict-closing resolver for instrument,
  taxonomy and trading-session consumers, including freshness schedule production and
  official ingestion.
- Exclude absent/unknown taxonomy from valuation peers; require positive official
  shares for every included current-session peer and use only official market-cap
  weighting.
- Hash-pin an exact applied catalog oracle containing every default expression and full
  immutable trigger definition/state.

## Confirmed closure and residual blockers

Round 113 corrected baseline identity, provider/auth precedence, exact constraint
definitions, revision-parameter fail-closed behavior, FULL-authoritative LIGHT leaf
fill, DecisionEnvelope action authority and the disabled production boundary are
closed. Acceptance `1.45.1` remains 308 unique cases with the `260/28/20` track split;
the independently recomputed active graph remains
`55ee5844b27767a4f231fa1732224b79ce22cf3ec5f18fe8e19b82c959ebfb14`.

The protected product trace was 260/260 but does not overrule the seven contract
findings. Non-fabricated elapsed cohorts remain unavailable. No production migration,
credential installation, runtime activation, deployment, LINE dispatch or promotion
was authorized or performed.
