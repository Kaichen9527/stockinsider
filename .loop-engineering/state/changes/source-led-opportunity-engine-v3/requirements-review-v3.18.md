# V3.18 host-compatibility Requirements closure

Date: 2026-08-22
Review authority: one fresh, read-only Requirements closure after rebasing the
frozen V3.18 implementation onto the protected host-compatibility bootstrap.
Result: `PASS`
Findings: `P0=0 P1=0 P2=0`

## Immutable subject

- Protected implementation parent: `d719d6dace387cdf10917ba7553791d2d4c0ec3e`
- Final repair-closure commit/tree: `ca9223b843d205939be17e706d7b781a6a25b309` / `e71bbf5f74707477d0f9366d845dad2d9163c97f`
- Full reviewed range: `d719d6dace387cdf10917ba7553791d2d4c0ec3e..ca9223b843d205939be17e706d7b781a6a25b309`
- Active graph: `ac0939699556262983a21be5330c7b4786fb309fd051883efe62c6ea672b7688`

## Requirements closure

The V3.18 implementation remains source-led: only approved, entity-linked,
public-source evidence can nominate a research candidate. Official TWSE, TPEx
and MOPS facts can validate, enrich or reject a nominated candidate, but cannot
quietly turn into all-market discovery. Paid InvestAnchors text and Telegram
content remain outside acquisition; unavailable third-party OAuth produces a
typed terminal outcome, never fabricated content.

`CandidateLedgerV318` retains cited last-good candidates for exactly twenty
completed source sessions. Retained candidates reserve the bounded funnel before
fresh candidates are admitted, so a fresh 60-item batch cannot silently erase
last-good research. The same frozen-cutoff retry does not consume another
session; a capacity loss is typed as
`candidate_capacity_reserved_for_retention`; only session 21 expires a retained
candidate. One evidence or fact deficit only downgrades that card to a typed
research/readiness state, while a checksum or integrity conflict alone may fail
closed globally.

The unique Decision Envelope remains the sole action authority. Research
readiness controls visibility and lane placement only: missing valuation,
incomplete technical history, stale sources, absent facts or unavailable action
authority cannot become a buy-like recommendation. Every visible card carries a
single decision revision, a bounded landing representation and its matching
full dossier; missing values remain explicit blockers rather than being
translated to `avoid`.

The rebase includes the signed, observed Codex `0.149.0-alpha.4` host pin in
`model-runner-host-pins-v3.11`, and the base-owned external gate now selects
only the immutable V3.18 Requirements, Architecture and exact-review refs. The
runtime bundle tracks the V3.18 dossier code. No database password reset,
credential rotation, LINE, dispatch, automated trading, V3 activation or
Promotion is introduced.

## Executable evidence examined

- `npm run test:source-led-opportunity-v3`: `61/61` PASS.
- `npm run test:model-runner-v3`: `20/20` PASS on the normal verified host.
- `npm run v3:doctor -- --expect-mode disabled --require-host-pin
  model-runner-host-pins-v3.11`: PASS.
- The base-owned protected external-worker regression suite is preserved for
  the host bootstrap and its V3.18 evidence binding.

This closure authorizes exactly one independent Architecture closure over the
carried Requirements evidence. It does not authorize migration, runtime
activation, production deployment, automatic trading, or a claim that future
returns have been proven. Evaluation governance remains
`blocked/non_fabricated_elapsed_cohorts_unavailable` until real cohorts mature.
