# V3.15 protected Requirements compatibility evidence

Date: 2026-08-13
Review authority: fresh Requirements Round 146, carried at the protected worker's
stable compatibility path without changing the reviewed active artifact graph.
Result: `PASS`
Findings: `P0=0 P1=0 P2=0`

## Immutable subject

- Protected implementation parent: `e74b672cf397b7f8ba11f86ff49b2633afb5b7dd`
- Final repair-closure commit/tree: `836a3a82c13c51a506d2113709f1e94a1f166a43` / `e813d2e034d28a46c6f3da86fdc8de9c34bbb8c4`
- Full reviewed range: `e74b672cf397b7f8ba11f86ff49b2633afb5b7dd..836a3a82c13c51a506d2113709f1e94a1f166a43`
- Active graph: `734b013bdfd750bfdf87ceb731f9db5033d9d4c8614323e1a884d8b43cb7c717`
- Canonical evidence: this protected Round 146 compatibility carrier.
- Acceptance inventory: `1.46.0`, 320 cases, partitioned as 272 product/runtime,
  28 model-runner and 20 evaluation-governance owners.

## Closure

Round 146 returned PASS for the bounded official-market research entrance, retained
single decision authority, point-in-time revenue and adjusted-price requirements,
narrow Supabase REST transport, additive migration and explicit no-buy-quota rule.
The earlier exact review found future filing leakage and a doctor privilege mismatch;
both remain closed. Production rehearsal then exposed one legitimate historical
predecessor (`1.44.6` plus its comparison identity) that the V3.14 migration did not
accept. The repaired migration now has a closed two-predecessor upgrade matrix:
`1.44.6` or `1.45.1` advances to `1.46.0`, preserves historical projection rows, and
still rejects zero, duplicate or mixed identities. A production-shaped PostgreSQL
regression downgrades all four owning functions, applies the upgrade twice, and proves
the canonical result. Production activation then found that the authorized immutable
source corpus legitimately exceeded the V3.11 1,000-family per-connector ceiling
(PTT 1,191; BullTalk 1,060). The V3.15 upgrade now conserves up to 2,000 ordered
families per connector and still raises an explicit conflict above that closed bound;
it never truncates silently. The resulting 3,385 selected revisions remain below the
12,000-job DAG conservation cap. The prior exact-attestation artifacts were then removed as
superseded evidence so the protected worker can require a new three-file immutable ADD;
that evidence-only topology change does not alter a product requirement.

The protected product gate then exposed one registry-oracle drift: the V3.15 correctness suite was present in package.json but absent from the frozen script-value row, and the active-graph oracle still named its prior digest. The repair binds the complete V3.15 suite, its canonical script-row digest and the operative V3.15 task/status state; the focused protected-oracle regressions now pass.\n\nThe coordinated activation rehearsal then exposed a bootstrap cycle: the old read-only projection had to be replaced by the new producer before the installer allowed that producer to start. The repair permits activation only when the new Web consumer is exact, the old projection checksum is valid, freshness is fresh/stale and the only health reasons are readonly projection compatibility/staleness; every other doctor finding still rolls back. The recorded health remains degraded until the new producer publishes the same release identity.\n\nThe final subject passes typecheck, lint, production build, base 61/61, product
correctness 93/93, migration 52/52, legacy 2/2, Playwright 8/8, performance 4/4,
model-runner 17/17 and disabled host-pin v3.8 doctor. The official TPEX request and
unit interpretation were also checked against the live provider response. These local
executions do not replace the protected external rerun.

The first coordinated production producer run then exposed an exact duplicate active
roster head for 2472 across two bounded frozen authority pages. Both rows derived the
same deterministic claim and mention identity, so downstream uniqueness correctly
failed closed. The repair collapses only identical claim identities before the
existing 200-entity bound; it does not merge distinct identities, change stock
authority, manufacture a claim or alter the 60→30→20 funnel. The production-shaped
2472 regression proves one candidate, one linked claim and one linked entity outcome.
Focused V3.15 tests pass 10/10 and the complete product-correctness suite passes 93/93.

Evaluation governance remains `blocked/non_fabricated_elapsed_cohorts_unavailable`;
no synthetic cohort, LINE/dispatch, automatic trade or Promotion authority is claimed.
This evidence grants only Requirements eligibility for Architecture, exact review and
the protected Code Gate.
