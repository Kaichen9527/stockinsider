# V3.16.12 Candidate Fact-Plane Bound Amendment

Production recovery run `4632f755-7faa-1ea3-0dac-68a16be485ef` completed all
3,424 immutable mention shards and the candidate funnel, then reproduced
`bound_violation` while claiming `facts_refresh`. Read-only sizing proved that
the unmodified V3.1 session-by-session corporate-action evidence exceeded the
6 MiB helper bound. Five candidates produced 1,243,797 bytes; ten produced
2,126,217 bytes before restoring the complete candidate ledger.

The authoritative discovery ledger remains bounded at 60 and is preserved
byte-for-byte in `candidateResult`. Expensive fact authority is now restricted
to the first ten deterministic `deepSelected` candidates. Those ten retain the
complete V3.1 adjusted-price evidence; all other candidates remain visible and
must be classified as unavailable until a later run selects them for deep
research. The wrapper rejects any final claim plane above 3 MiB and keeps the
prior helper private from the legacy scheduler role.

This amendment does not increase a SQL or product bound, compress evidence,
enable LINE/dispatch, enable V3 Promotion, or claim validated future returns.
