# Taiwan strategy lab — final-phase handoff

This document is a preparation checklist. The bounded research task performs no capacity purchase, host cleanup, merge, production database write or deployment. A completed lab study or elapsed ten-hour schedule is not release admission.

## Verified baseline and inherited release work

GitHub `main` was verified as `169aad1b6cfa747f78ae3464b614f43c0749d806` for this task. PR #279 merged the research-only entry-plan/authority work; #281 repaired the protected authority graph/digest declarations; #282 fixed standalone packaging and symlinked verifier entrypoints. The final heads of #279 and #282 have successful protected root checks. These checks do not establish which commit runs on Contabo or whether official data and articles have been refreshed.

The existing entry-plan handoff's old missing-graph paragraph is superseded by #281 and the current protected worker registration. Ruleset `20177392` was observed active, requiring `stockinsider-v3-gate-root`, with no bypass actor. Re-read current state before final integration. A new strategy-lab implementation needs its own applicable Requirements, Architecture, product/model checks and exact-commit review; do not copy old attestations, disable gates, or turn a favorable experiment into a gate PASS.

## Research completion package

Provide the exact code commit, registry and data hashes, all seven hypothesis statuses, all 15 registered price/scenario trials and failures, descriptive half-year development-window definitions, assumptions, and net metrics/cost diagnostics with limitations. Continuous development windows are not independent OOS validation; a same-fill-path cost addback is not a true counterfactual gross portfolio. The study population is the frozen eight-survivor panel until genuine broader point-in-time membership is available. S5/S7 stay blocked without their required event evidence. The 2024+ holdout remains unopened in this round.

Provide candidate-input snapshot identity/cutoff/count, article preview_ready/blocked counts, missing/duplicate/error counts and the dry-run queue manifest. Preview readiness requires per-symbol terminal coverage; shared portfolio returns are never individual-stock performance. State plainly that drafts describe historical research, provide no current entry points and have not updated production articles. A missing complete candidate export cannot be replaced with the survivor panel or a handful of public cards. `GET /api/internal/candidate-dossier-bundle` is not an export; it writes bundle/outbox rows and is not used by the lab.

## Capacity and deployment sequence

1. Finish and independently review the offline research and concrete integration proposal. Keep production strategy/model semantics unchanged unless a separately reviewed version implements an explicitly selected change. Existing entry plans remain `research_only`; simulated results do not authorize brokerage execution or relax liquidity/formal gates.
2. Re-inventory Contabo using `deployment/vps/CAPACITY_AND_BACKUP.md` and the reviewed host tools. Hold the host heavy-operation and StockInsider build/restore/backfill locks during admission. Inventory must be fresh; declare concurrent database, WAL, temporary/index, documents, deployment, staging and growth budgets.
3. Preserve at least 15 GiB free disk throughout and 1.5 GiB available memory after the declared workload; 20 GiB disk is the warning threshold. The last reported projected disk reserve was about 9.9 GiB, below the hard floor. Earlier documentation reporting 24.05 GiB free is historical and cannot authorize this operation. Determine any expansion from a fresh measurement and concrete reviewed capacity proposal; do not purchase storage or delete another application's data in this research phase.
4. Once the final operational action is authorized and all admission checks pass, package the exact clean reviewed source with the reviewed packager. Bind both commits and the final file inventory to the standalone manifest; verify direct and symlinked entrypoints. Retain current/previous rollback identities and backups. Never infer a Vercel production cutover from the older project overview.
5. For the inherited entry-plan deployment, verify/apply `20260924_entry_plan_official_action_symbols.sql` only through the user-authorized additive `db:v3:apply-reviewed` path and its valid exact-commit attestation. No raw `psql`, generic migration runner or speculative schema changes. This lab introduces no production schema requirement of its own.
6. Continue official entry-plan authority repair through authenticated `POST /api/internal/candidate-history-backfill` with `purpose: entry_plan_authority`, bounded `requestBudget` and the same returned `runId`. The endpoint requires the active VPS writer and fixed principal. It freezes a current common-stock roster and the last 240 completed sessions; it does not supply multi-year research history or publish articles. Require pending/retry/running counts zero and no failed jobs, while preserving explicit listing/suspension/data gaps.
7. Run the full authenticated current core pipeline with the existing writer lease, without a symbol canary scope or `dryRun`, and explicitly avoid `skipIfResearchSessionComplete` for initial current acceptance. Historical resume remains separate and must not replace the current public snapshot. In-progress or failed authority repair must preserve the prior snapshot.
8. Verify the complete actual candidate roster, authority coverage, saved revisions, stage summaries, current detail routes and publication receipts. Require `expectedCount = evaluatedCount + dataInsufficientCount` with zero missing/duplicate/invalid/unavailable/failed plan outcomes. Data-insufficient is an accounted result, not a usable trade zone. The 100,000-row source/history bounds fail closed; record live pages, rows and duration.
9. Any later article publication must consume the real complete snapshot through the existing guarded server path, bind each article to its reviewed evidence/revision and preserve failures. A dry-run queue is not a production write receipt. Report exact deployed commit, migration/repair/research/publication IDs, timestamps, health checks and rollback readiness before claiming the site is updated.

## What this session can and cannot establish

The Chat workspace can implement/test the isolated lab, analyze acquired development data, prepare articles, build and produce reviewable artifacts. Only actual authenticated environment operations can establish the complete database export, current VPS capacity, active release identity, migrations, official authority coverage and published articles. No current live capacity, deployment or full-candidate update is attested by this handoff.
